# T-SSA サイト運用仕様書（AI協働者向け）

この文書は、T-SSA公式サイトの変更を担当するAI協働者のための運用仕様書です。ユーザーからの最新の指示を最優先しつつ、ここに書かれた方針を守ってください。

## 1. このサイトの目的

T-SSA（Team Sanaru Super Athlete）は、浜松市を拠点とする小学生・中学生向け陸上競技クラブです。このサイトの当面の目的は、クラブの想い・活動概要を伝え、体験参加や問い合わせにつなげることです。

- 公開サイト: <https://t-ssa-hamamatsu.web.app/>
- GitHubリポジトリ: <https://github.com/kaomatu/t-ssa-website>
- FirebaseプロジェクトID: `t-ssa-hamamatsu`
- データベース・ログイン・会員管理機能は、現在使わない

## 2. 現在の掲載内容

- キャッチコピー: 「一人ひとりの可能性を、未来の力に。」
- 対象: 小学4年生〜中学3年生
- 活動時間: 毎週土曜日 9:00〜12:00
- 活動場所: 入野中学校グラウンド、協働センター体育館ほか
- 初心者歓迎、体力づくり・技術向上・仲間づくりを大切にする方針
- 問い合わせ先は現在ダミーの `info@example.com`

### 表示しない情報

- 会費・金額（コーチの意向により掲載しない）
- 実在の連絡先、住所、申込URLなど、ユーザーから確定情報を受け取っていないもの

## 3. デザイン方針

- シンプルで親しみやすい、スマホファーストのデザイン
- 主な色はネイビー・青・黄色
- 奇抜な演出より、読みやすさ・問い合わせの分かりやすさを優先する
- 正式ロゴは未提供。現在のTマークは仮のもの
- トップ写真スライドの5枚はAI生成のダミー画像。実際の活動写真を使うときは、本人・保護者・撮影者など必要な許可が取れていることをユーザーに確認する

### レスポンシブの基準

| 画面幅 | レイアウト方針 |
| --- | --- |
| 〜699px | スマホ向けの1列レイアウト |
| 700〜1023px | タブレット向け。ヒーローは縦配置、活動概要は3列、特色カードは2列＋横長カード |
| 1024px〜 | PC向け。ヒーロー・各セクションを2列または3列で構成 |

変更後は、少なくともスマホ幅（390px程度）・タブレット幅（800px程度）・PC幅（1280px程度）で、横方向のはみ出しや文字の欠けがないか確認してください。

## 4. 技術構成

- Next.js 16（App Router）
- React 19 / TypeScript
- Tailwind CSS 4を導入済み。ただし画面固有のデザインは主にCSSで実装
- 静的エクスポート（`output: "export"`）
- Firebase Hosting
- Node.js / npm

データベース、Firebase Authentication、Firebase SDK、バックエンドAPIは使っていません。

## 5. 主なファイル

| 目的 | ファイル |
| --- | --- |
| トップページの文章・構成 | `app/page.tsx` |
| 全体の見た目・レスポンシブ設定 | `app/globals.css` |
| トップの写真スライド | `components/HeroCarousel.tsx` |
| プライバシーポリシー | `app/privacy/page.tsx` |
| ダミー写真 | `public/images/hero/` |
| Firebase Hosting設定 | `firebase.json` / `.firebaserc` |
| ビルド・公開コマンド | `package.json` |
| 公開・ローカル確認の概要 | `README.md` |

## 6. 変更時のルール

1. ユーザーの依頼内容に必要な範囲だけを変更する。
2. 活動場所、時間、対象、連絡先、コーチ情報、費用、規約などの事実は推測で追加・変更しない。
3. 外部フォーム・Googleサービス・データベースを勝手に追加しない。必要なら提案し、ユーザーの指示を待つ。
4. 実在の人物写真、氏名、メールアドレスなどの個人情報は、ユーザーが明確に提供・掲載を指示したものだけを使う。
5. 正式ロゴが提供されたら、仮のTマークを置き換える。ロゴそのものをAIで新規作成して正式ロゴ扱いにしない。
6. 金額は掲載しない。ユーザーが明確に方針を変更した場合だけ再掲載する。
7. ダミー画像を実画像へ差し替える場合は、`public/images/hero/` の同名JPEGを置き換えるとスライド構成を保てる。

## 7. 変更後の確認手順

コード・デザインを変更したら、原則として次を実行してください。

```bash
npm run lint
npm run build
```

ローカル表示を確認する場合:

```bash
npm run dev
```

`http://localhost:3000` を開き、終了時はターミナルで `Control + C` を押します。

`npm run build` の結果は `out/` に出力されます。このフォルダは自動生成物なのでGitには含めません。

## 8. GitHubとFirebaseの運用

### GitHub

- 既定ブランチは `main`
- 通常の変更は、内容を確認してコミットし、`main` へpushする
- `node_modules/`、`.next/`、`out/`、`.firebase/`、`.env`系ファイルはコミットしない
- APIキー、パスワード、認証トークン、個人情報は絶対にコミットしない

### Firebase Hosting

公開前に、必ずLintとビルドを成功させます。

```bash
npm run deploy
```

このコマンドはビルド後、Firebase Hostingへ公開します。ユーザーが「公開」「反映」「デプロイ」などを明示したときだけ実行してください。コードの変更・GitHubへのpushだけでは、公開サイトは更新されません。

## 9. 今後の拡張について

ブログはまだ実装していません。将来追加する場合も、当面はデータベースを使わず、Googleドキュメントで記事を作成し、静的ページへ変換してFirebase Hostingへ公開する方式を基本案とします。

Googleフォームの申込み、Google Workspace、Google Classroom、Google Chatなどは、クラブ運営の情報が確定してから別途追加を検討します。現在の集客サイトに、これらの仕組みを前提としたコードは追加しません。

## 10. ユーザーへの報告方法

変更が完了したら、次を簡潔に報告してください。

- 何を変更したか
- Lint・ビルド・表示確認の結果
- Firebaseへ公開したか、または公開していないか
- ユーザーが次に用意すべき情報があれば、その内容
