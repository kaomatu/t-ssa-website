# 陸上クラブチーム Webサイト・メール基盤 設計書

## 1. プロジェクト概要
- **目的**: 2〜5名のコーチ陣で運営する陸上競技クラブチームの公式Webサイトおよび専用メールアドレスの開設。
- **要件**:
  - 対外的な信頼性を担保するため、独自ドメインを運用する。
  - Webサイトは「見やすさ」と「レスポンスの速さ（表示速度）」を最重要視する。
  - コーチ陣（2〜5名）が共同でメール対応できる体制を構築する。

## 2. インフラ・ネットワーク設計
- **採用サーバー**: ConoHa WING（WINGパック ベーシックプラン）
  - 選定理由: 表示速度が高速（Nginx + NVMe SSD）、独自ドメインが永年無料、直感的な管理画面。
- **ドメイン**: WINGパックの無料特典を利用して取得（`.com`, `.jp` 等）。
- **SSL/TLS**: ConoHa WING標準の無料独自SSL（Let's Encrypt）を適用。

## 3. メールシステム設計
- **メールアドレス構成（例）**:
  - 総合窓口: `info@yourdomain.com` または `contact@yourdomain.com`
- **運用フロー（コーチ2〜5名での共有）**:
  - **IMAP共有方式（推奨）**: 各コーチのPC/スマホ（標準メール、Outlook、Thunderbird等）にIMAPでアカウントを設定し、既読・返信ステータスを全員で同期する。
  - **代替方式**: 代表アドレス宛のメールを各コーチの個人アドレス（Gmail等）へ自動転送する。
- **送信ドメイン認証（セキュリティ対策）**:
  - **SPF**: ConoHa WING標準設定を利用（`v=spf1 include:_spf.conoha.ne.jp ~all`）。
  - **DKIM**: コントロールパネルよりONに設定。
  - **DMARC**: DNSのTXTレコードに `_dmarc` 情報を追加。

## 4. Webサイト（フロントエンド）設計
- **コア技術**: Next.js (App Router) + TypeScript
- **UI / スタイリング**: Tailwind CSS
- **レンダリング方式**: Static Export (`output: 'export'`)
  - 選定理由: Reactエコシステムを活用しつつ、完全な静的ファイル（HTML/CSS/JS）としてビルドすることで、ConoHa WING環境下での初回表示速度（LCP）を最速にするため。
- **画像最適化**: Next.jsの機能を利用せず（`unoptimized: true`）、ビルド時にWebP等に最適化して静的配置する。
- **フォーム機能**: 静的サイトのため、バックエンド処理が不要な外部フォーム代行サービス（Formspree, SSGform 等）や、公式LINEへの誘導を採用。

## 5. サイトマップ構成（案）
- `/` : トップページ（ヒーロー、活動理念、お知らせ、体験案内）
- `/about/` : クラブ紹介（対象年齢、練習場所・時間など）
- `/coaches/` : コーチ陣の紹介
- `/schedule/` : 練習予定・年間スケジュール
- `/fee/` : 料金体系（月謝・入会金・保険等）
- `/contact/` : お問い合わせ・体験申し込み（外部フォーム）

## 6. 開発・デプロイメントフロー
1. **ローカル開発**: Next.jsにてコンポーネント開発。
2. **ビルド**: `npm run build` を実行し、`out/` ディレクトリに静的ファイルを書き出し。
3. **デプロイ**: ConoHa WINGのパブリックディレクトリ（`public_html/yourdomain.com/`）へ、生成された `out/` の中身をFTP/SFTPでアップロード。
4. **配信最適化**: `.htaccess` を用いてブラウザキャッシュと通信圧縮（Gzip/Brotli）を有効化し、更なる高速化を図る。
